[project]
name = "careercoachai"
version = "0.2.0"
description = "AIApply-like automated job application platform"
requires-python = ">=3.10"
dependencies = [
  "openai>=1.60.0",
  "playwright>=1.50.0"
]

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
